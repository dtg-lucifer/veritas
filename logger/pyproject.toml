[project]
name = "logger"
version = "0.1.0"
description = "Pyshark Network Packet Sniffer & Redis Message Queue Producer for Internal Firewall"
readme = "README.md"
authors = [
    { name = "dtg-lucifer", email = "piushbasu@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyshark>=0.6",
    "redis>=5.0.0",
    "rich>=13.7.0",
    "typer>=0.12.0",
    "pydantic>=2.8.0",
]
